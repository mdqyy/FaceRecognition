num = size(svmDebug,1);
mess = zeros(num,1);
for ii = 1: num
    mess(ii) = sum( svmDebug(ii,2:end-1) > svmDebug(ii, svmDebug(ii,1)+1));
end
    
for ii = 1:num
    tmp = zeros(1, 60);
    tmp(svmDebug(ii,1) + 1) = svmDebug(ii, svmDebug(ii,1) + 1);
    bar(svmDebug(ii,2:end-1));
    
    saveas(gcf, num2str(ii), 'bmp');
end